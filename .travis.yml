language: python

python:
  - "3.3"

branches:
  only:
    - api-stormclustering-release

sudo: required

before_install:
  # Install ZooKeeper
  - "sudo apt-get install zookeeperd"

# command to install dependencies
install: "pip install -r stormclustering/requirements.txt"

before_script:
  - "sudo service zookeeper init"
  - "sudo service zookeeper start"

before_script:
  - pip install virtualenv
  - cd stormclustering
  - virtualenv env
  - source env/bin/activate
  - pip install requests
  - pip install httplib2
  - pip install nose
  - pip install Flask-SQLAlchemy
  - pip install kazoo

script: nosetests

after_script:
  - "sudo service zookeeper stop"

services:
  - docker

after-success:
  - cd ..

before_deploy:
  - cd /home/travis/build/airavata-courses/TeamAviato
  - zip -r SGA_TeamAviato_StormClustering.zip stormclustering scripts appspec.yml || true
  - mkdir -p "stormClustering"
  - mv SGA_TeamAviato_StormClustering.zip stormClustering/SGA_TeamAviato_StormClustering.zip || true

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket-ohio
    local_dir: stormClustering
    region: us-east-2
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on: 
      branch: api-stormclustering-release
     
  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket-ohio
    key: SGA_TeamAviato_StormClustering.zip
    bundle_type: zip
    application: TeamAviatoApp-ohio
    deployment_group: TeamAviatoApp-ohio
    region: us-east-2
    skip_cleanup: true
    on:
      branch: api-stormclustering-release

  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket-ohio
    local_dir: stormClustering
    region: us-east-2
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on:
      branch: api-stormclustering-release
      
  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket-ohio
    key: SGA_TeamAviato_StormClustering.zip
    bundle_type: zip
    application: TeamAviatoApp-ohio2
    deployment_group: TeamAviatoApp-ohio2
    region: us-east-2
    skip_cleanup: true
    on:
      branch: api-stormclustering-release
      
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket2
    local_dir: stormClustering
    region: us-west-2
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on:
      branch: api-stormclustering-release

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: teamaviatobucket2
    key: SGA_TeamAviato_StormClustering.zip
    bundle_type: zip
    application: TeamAviatoApp
    deployment_group: TeamAviatoApp
    region: us-west-2
    skip_cleanup: true
    on:
      branch: api-stormclustering-release
      

after_deploy:
  - echo "Performing after deploy procedures..."
  - done
  
notifications:
  email:
    - laxmibh.malkareddy@gmail.com
    
